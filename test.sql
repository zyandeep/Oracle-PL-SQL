SET SERVEROUT ON

CREATE OR REPLACE FUNCTION TEST(NAME VARCHAR2)
RETURN NUMBER
IS

VAR_Q NUMBER := 0; 

BEGIN

SELECT QUANTITY INTO VAR_Q FROM EQUIPMENT WHERE UPPER(E_NAME) = UPPER(NAME);


RETURN NVL(VAR_Q, 0) + 1);

END;
/
